# Copy to .env.local for local development. Never commit the real value.
# The deployed site sets this as a server-side environment variable in the
# hosting provider's dashboard (e.g. Vercel Project Settings > Environment
# Variables) — it is never exposed to the browser.
ANTHROPIC_API_KEY=
